/**
 * Mock for @plausible-analytics/tracker
 * Used in test environment to avoid import errors
 */

// Simple mock functions - they'll be replaced by actual mocks in tests if needed
export const init = () => {};
export const track = () => {};

